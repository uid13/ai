# Redis 配置
spring:
  data:
    redis:
      host: localhost
      port: 6379
      # 可根据需要添加更多 Redis 配置
      # password: ENC(加密后的密码)
      # database: 0
      # timeout: 5000ms
      # lettuce:
      #   pool:
      #     max-active: 8
      #     max-idle: 8
      #     min-idle: 0
